---
interface Props {
  class?: string;
  size?: 'sm' | 'md' | 'lg' | 'full' | 'wide';
}

const { class: className = '', size = 'full' } = Astro.props;


const sizes = {
  sm: 'max-w-3xl',
  md: 'max-w-[1152px]',
  lg: 'max-w-[1280px]',
  full: 'max-w-[1400px]',
  wide: 'max-w-[1536px]',
};

---

<div class={`w-full mx-auto px-2 sm:px-4 lg:px-6 ${sizes[size]} ${className}`}>
  <slot />
</div>

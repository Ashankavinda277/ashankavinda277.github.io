---
import { badgeClass, type BadgeVariant } from './badgeStyles';

interface Props {
  variant?: BadgeVariant;
  class?: string;
}

const { variant = 'default', class: className = '' } = Astro.props;
---

<span class={badgeClass({ variant, className })}>
  <slot />
</span>
